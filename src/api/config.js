export const config = {
  ROOT_URL: "https://jsonplaceholder.typicode.com/",
};
export const apiUrls = {
  Users: `${config.ROOT_URL}users/`,
  Albums: `${config.ROOT_URL}albums/`,
  Photos: `${config.ROOT_URL}photos/`,
};
